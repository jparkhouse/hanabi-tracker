# Workflow for deploying from testing branch to /hanabi-tracker/test
name: Deploy to GitHub Pages (Testing)

on:
  push:
    branches:
      - testing
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-testing"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Save Build to gh-pages-testing Branch
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git fetch origin
          git checkout gh-pages-testing || git checkout --orphan gh-pages-testing
          rm -rf *
          cp -r dist/* .
          git add .
          git commit -m 'Deploy to GitHub Pages from testing branch'
          git push origin gh-pages-testing
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
